BACKEND_PORT=8000
FRONTEND_PORT=8080

# Build-time frontend variables used by Vite in Docker build.
# Use host-accessible backend URL because the browser runs outside Docker networking.
FRONTEND_VITE_API_URL=http://localhost:8000
FRONTEND_VITE_API_TIMEOUT=30000
